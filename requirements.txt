# Browser automation — drives Chrome for login, search, apply flows
playwright>=1.44

# Patches Playwright pages to evade common bot-detection fingerprints
# (webdriver flag, chrome.runtime, plugins array, etc.)
playwright-stealth>=1.0.6

# Loads .env credentials and config without touching the shell environment
python-dotenv>=1.0

# YAML selector files (platforms/selectors/*.yaml) loaded by core/selectors.py
PyYAML>=6.0

# Test runner — scoring logic, dedupe, CSV writes have unit tests
pytest>=8.0
# Async test support for platform orchestration smoke tests
pytest-asyncio>=0.23

# Outreach: Gmail API send via OAuth
google-api-python-client>=2.0
# Outreach: per-inbox OAuth consent flow (setup_gmail_oauth.py)
google-auth-oauthlib>=1.0
# Outreach: Gmail API auth transport
google-auth-httplib2>=0.2
